import { IPagination } from "@/components/main/evants/evants";

export const pagination: IPagination = { // const for pagination events and reports
    limit: 3, // limit for view items
    offset: 3, // view items
    page: 1 // default page
}